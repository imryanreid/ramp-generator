// ==============================================
// TOOL MARK
// Each tool's favicon, inline, at whatever size the
// caller asks for.
//
// Inline SVG rather than <img src="https://…/favicon.svg">
// on purpose. A remote icon is a cross-origin request
// per row for a 16px glyph, it fails offline and on a
// preview deploy, and the unreleased tools have no
// domain to load one from. These are a handful of
// shapes; shipping them as markup costs less than the
// requests would.
//
// Every mark shares the plate: a #131210 rounded
// square with the same rx and the same 6px inset, so
// the family reads as a set and only the figure
// inside changes.
//
// SHARED FILE. Authored in ramps-studio, copied
// outward. Don't edit it downstream.
// ==============================================
import { cn } from "../utils"

const INK = "#131210"

/**
 * Figures, in the 32-unit viewBox the plate defines.
 *
 * A tool with no mark yet renders the bare plate rather than a placeholder
 * glyph — an invented symbol for an unbuilt tool is a promise about what it
 * will be, and these are quiet enough to read as "not yet".
 */
const FIGURES: Record<string, React.ReactNode> = {
  ramps: (
    <>
      <rect x="6" y="6" width="20" height="4" rx="1.2" fill="#c5d9ff" />
      <rect x="6" y="11" width="20" height="4" rx="1.2" fill="#8db0ff" />
      <rect x="6" y="16" width="20" height="4" rx="1.2" fill="#3d7dff" />
      <rect x="6" y="21" width="20" height="4" rx="1.2" fill="#2452b0" />
    </>
  ),
  // Five bars at irregular heights. Same shape as beeps.studio/favicon.svg,
  // generated by that repo's scripts/build-icons.py — change one, change both.
  //
  // It was a decaying oscillation drawn as one continuous stroke, which read
  // as a near-twin of the motion curve below: same colour, same weight, same
  // kind of drawing. Bars are a different object at a glance, and they rhyme
  // with Ramps' four horizontal bars turned a quarter turn.
  //
  // The heights are deliberately IRREGULAR — a smooth fall reads as a level
  // meter, and real audio is not monotonic.
  sound: (
    <>
      <rect x="6.00" y="11.50" width="2.8" height="9" rx="1.1" fill="#8db0ff" />
      <rect x="10.30" y="6.00" width="2.8" height="20" rx="1.1" fill="#8db0ff" />
      <rect x="14.60" y="10.00" width="2.8" height="12" rx="1.1" fill="#8db0ff" />
      <rect x="18.90" y="7.50" width="2.8" height="17" rx="1.1" fill="#8db0ff" />
      <rect x="23.20" y="12.50" width="2.8" height="7" rx="1.1" fill="#8db0ff" />
    </>
  ),
  // A spring's step response, two-tone and deliberately underdamped. Same
  // point list as springs.studio/favicon.svg — change one, change both.
  //
  // A single 3.4-wide stroke carried far less weight than Ramps' four filled
  // bars, so the two did not read as equals side by side. Filling the area
  // beneath was tried first and read as a solid blob rather than a curve; a
  // heavy stroke with a lighter core gives it mass while keeping it a LINE,
  // which is what the mark is about. Two tones from one ramp is where Ramps'
  // mass comes from.
  //
  // Damping is zeta = 0.13 — low enough to overshoot and ring twice before
  // settling. The earlier curve eased once and spent half its width on a flat
  // tail, which wasted the plate and undersold what a spring does.
  motion: (
    <>
      <polyline
        points="7.00,24.40 7.20,24.28 7.40,23.93 7.60,23.36 7.80,22.60 8.00,21.68 8.20,20.61 8.40,19.43 8.60,18.18 8.80,16.88 9.00,15.56 9.20,14.26 9.40,13.01 9.60,11.83 9.80,10.75 10.00,9.79 10.20,8.96 10.40,8.28 10.60,7.76 10.80,7.41 11.00,7.22 11.20,7.20 11.40,7.34 11.60,7.62 11.80,8.05 12.00,8.59 12.20,9.24 12.40,9.98 12.60,10.78 12.80,11.63 13.00,12.49 13.20,13.36 13.40,14.21 13.60,15.03 13.80,15.79 14.00,16.48 14.20,17.08 14.40,17.60 14.60,18.01 14.80,18.31 15.00,18.50 15.20,18.59 15.40,18.56 15.60,18.43 15.80,18.21 16.00,17.89 16.20,17.51 16.40,17.05 16.60,16.55 16.80,16.01 17.00,15.44 17.20,14.86 17.40,14.29 17.60,13.73 17.80,13.21 18.00,12.72 18.20,12.28 18.40,11.90 18.60,11.59 18.80,11.34 19.00,11.17 19.20,11.07 19.40,11.04 19.60,11.08 19.80,11.19 20.00,11.36 20.20,11.59 20.40,11.86 20.60,12.18 20.80,12.52 21.00,12.89 21.20,13.27 21.40,13.65 21.60,14.02 21.80,14.39 22.00,14.73 22.20,15.04 22.40,15.31 22.60,15.55 22.80,15.74 23.00,15.89 23.20,15.98 23.40,16.03 23.60,16.03 23.80,15.99 24.00,15.90 24.20,15.77 24.40,15.61 24.60,15.42 24.80,15.20 25.00,14.97"
        fill="none"
        stroke="#2452b0"
        strokeWidth="7.2"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
      <polyline
        points="7.00,24.40 7.20,24.28 7.40,23.93 7.60,23.36 7.80,22.60 8.00,21.68 8.20,20.61 8.40,19.43 8.60,18.18 8.80,16.88 9.00,15.56 9.20,14.26 9.40,13.01 9.60,11.83 9.80,10.75 10.00,9.79 10.20,8.96 10.40,8.28 10.60,7.76 10.80,7.41 11.00,7.22 11.20,7.20 11.40,7.34 11.60,7.62 11.80,8.05 12.00,8.59 12.20,9.24 12.40,9.98 12.60,10.78 12.80,11.63 13.00,12.49 13.20,13.36 13.40,14.21 13.60,15.03 13.80,15.79 14.00,16.48 14.20,17.08 14.40,17.60 14.60,18.01 14.80,18.31 15.00,18.50 15.20,18.59 15.40,18.56 15.60,18.43 15.80,18.21 16.00,17.89 16.20,17.51 16.40,17.05 16.60,16.55 16.80,16.01 17.00,15.44 17.20,14.86 17.40,14.29 17.60,13.73 17.80,13.21 18.00,12.72 18.20,12.28 18.40,11.90 18.60,11.59 18.80,11.34 19.00,11.17 19.20,11.07 19.40,11.04 19.60,11.08 19.80,11.19 20.00,11.36 20.20,11.59 20.40,11.86 20.60,12.18 20.80,12.52 21.00,12.89 21.20,13.27 21.40,13.65 21.60,14.02 21.80,14.39 22.00,14.73 22.20,15.04 22.40,15.31 22.60,15.55 22.80,15.74 23.00,15.89 23.20,15.98 23.40,16.03 23.60,16.03 23.80,15.99 24.00,15.90 24.20,15.77 24.40,15.61 24.60,15.42 24.80,15.20 25.00,14.97"
        fill="none"
        stroke="#8db0ff"
        strokeWidth="3"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </>
  ),
}

export default function ToolMark({
  id,
  size = 16,
  className,
}: {
  /** Tool id from the manifest. An unknown id renders the bare plate. */
  id: string
  size?: number
  className?: string
}) {
  return (
    <svg
      viewBox="0 0 32 32"
      width={size}
      height={size}
      aria-hidden="true"
      focusable="false"
      className={cn("shrink-0", className)}
    >
      <rect width="32" height="32" rx="7" fill={INK} />
      {FIGURES[id] ?? null}
    </svg>
  )
}
